import * as React from 'react';

interface IProps {
  users: any;
}

const ChatHistory: React.SFC<IProps> = () => (
  (
    <div>
      <div>ChatHistory</div>
    </div>
  ));

export default ChatHistory;
